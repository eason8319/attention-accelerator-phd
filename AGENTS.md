# 项目协作规则

## 项目同步

- 本地 `F:\attention-accelerator-phd` 是项目权威版本；WSL 的 `/mnt/f/attention-accelerator-phd` 是同一目录。
- 服务器 `myserver:/cluster/home/zengy/attention-accelerator-phd` 用于实验执行，GitHub 用于版本共享；二者不得自动覆盖本地已有文件。
- 本地每个实验必须同时保有实验源码、必要运行依赖、原始结果和唯一正式报告。从服务器补齐缺失的实验源码、可移植配置/依赖声明，以及结果目录中的数据、图、必要日志和参数元数据；不能把实验源码误当作服务器专用文件排除。
- 不回收 Slurm/任务提交文件、服务器私有环境配置、集群说明、虚拟环境、模型缓存、凭据或 Git 内部数据。模型缓存留在各机 `HF_HOME`，见[模型权重缓存](#model-weight-cache)；CUDA/BitDecoding/SAW 隔离环境见[隔离 GPU 运行时](#isolated-gpu-runtimes)；不得把集群或本机权重或这些运行时复制进仓库或 `.server-sync/`。服务器报告不直接作为本地正式报告；以结果为依据在本地撰写。
- 本地不存在的文件按原相对路径补齐；同名同内容保留本地；同名异内容保留本地原文件，将服务器原件存入 `.server-sync/<同步用途>/conflicts/<原相对路径>`，登记差异，不自动合并。
- 回收前核对 `experiment.json` 的 `results.retired_files`：旧路径与 SHA-256 均匹配，且权威副本、归档成员或已验证的替代结果及其删除依据仍可核验时，跳过已去重、已归档、已淘汰内容，不重新补回。相同旧路径出现新内容时仍按新增证据核查并保全，不按目录名或日期直接忽略。
- 不得使用删除式镜像、强制重置或服务器到本地的覆盖式拉取。处理符号链接和路径冲突时必须先确保目标仍位于本地项目内。
- 每次回收记录实验源码与结果的来源、筛选范围、文件清单和 SHA-256；核验新增文件、冲突归档及原有文件保全。旧全项目快照仅作历史归档，不沿用其范围。
- 运行中实验只能记为带时间的快照；不得把部分结果称为完成。后续新增结果按同一规则回收。
- 缺失的通用实验依赖应补齐；若所需版本与本地共享模块冲突，将必要版本放在实验内的 `runtime/` 并明确入口引用，不覆盖本地主版本。运行入口使用项目相对路径，不能依赖服务器绝对路径。软件版本与资源要求必须注明，本地源码完整不等于无需安装依赖或无需 GPU。
- 本地完整回收不等于 GitHub 备份；GitHub 同步范围遵守下节及 `.gitignore`。
- Cursor 同时遵守 `.cursor/rules/local-authority-sync.mdc`；本规则适用于整个项目及后续所有同步任务。

<a id="model-weight-cache"></a>

## 模型权重缓存（本机与集群）

本节对本地 WSL/Windows 与集群 `myserver` 同等有效；作业、交互会话与回收任务均须遵守。

- 每台机器只使用一个 Hugging Face 缓存根目录，由环境变量 `HF_HOME` 指定。布局为 `$HF_HOME/hub/models--<org>--<name>/`。模型身份是仓库 ID 与 revision，不是某台机器上的绝对路径。
- 默认路径：本机 WSL `/mnt/f/hf-cache`（Windows `F:\hf-cache`）；集群 `$HOME/hf-cache`（当前账号即 `/cluster/home/zengy/hf-cache`）。登录节点预热、计算节点离线读取，均指向该机 `HF_HOME`。已设置的 `HF_HOME` 不得被脚本改写到另一台机器的路径（例如集群作业不得指向 `/mnt/f/hf-cache`）。
- 该目录维护 `INVENTORY.json`，登记 `id`、`revision`、用途角色、`hub_dir` 与权重哈希。新增、核验或删除快照后更新该机清单。本机与集群各有一份清单，不以同步文件代替下载。
- 权重、分词器快照与 Hub 数据集缓存不入库，不写入实验 `runtime/`、`.tools/`、仓库内 `.hf-cache/` 或 `.server-sync/`。服务器到本地的回收不得复制 `HF_HOME`；本机与集群需要同一模型时，按 ID 与 revision 在该机缓存中下载或核验，不靠复制权重复制对齐。
- 不得用 Instruct 快照代替 base，也不得用 base 代替 Instruct。R1 开发/冒烟使用 `Qwen/Qwen2.5-0.5B-Instruct`；R2 开发检查使用 `Qwen/Qwen2.5-0.5B`。正式评测模型按其协议 revision 放入同一 `HF_HOME`。残缺分片可删；仍被 R1/R2 使用的完整快照不得整理掉。不在仓库内再放一份平行模型目录。

<a id="isolated-gpu-runtimes"></a>

## 隔离 GPU 运行时（本机与集群）

本节对本地与集群 `myserver` 同等有效。步骤 9 口径以 R2 共享配置 `gpu_kernels` 为准，本规则管环境与文件，不替代该协议。

- CUDA 扩展编译、BitDecoding、SAW-INT4 不得装入 `r1-kv-baseline` 或步骤 2 探测所用 `torch 2.5.1+cu121`。不得为“对齐 CUDA”升级上述环境。作业不得在 `source research/r1_kv_baseline/activate.sh` 之后于同一 conda 里安装或编译这三者。
- 三者在**该机**各自使用隔离环境；虚拟环境、toolkit 与编译产物不入库，不写入 `.tools/`、实验 `runtime/` 或 `.server-sync/`，不从一台机器复制到另一台。本机与集群需要同一内核时，按协议固定提交在该机隔离环境中构建。
- CUDA：新建只用于编扩展的环境，`CUDA_HOME` 指向 nvcc 12.4，或换一套 toolkit 与**该隔离环境** PyTorch 一致的组合。
- BitDecoding：隔离 venv，固定提交见协议；补 `flash-attn` / `ninja`，按作者 `setup.py` 编 sm80。先最小 shape 功能冒烟，再执行协议 `gpu_timing`。量化网格若对不上项目 C2/C3，标成同 GPU 的作者格式，不得改名为 C2。
- SAW-INT4 官方 FA3 仅 H100/H800。无该设备时步骤 9 不报官方 kernel 时延或 TPS；C3 与本仓库旋转及 Q/O 变换对拍，论文与作者文档只作机制参照。更换 FA2 等后端必须标为适配，不能称官方复现。Flash-Decoding 与本仓库 C0 的 GPU 时延可在已核验 Ada 上测，不依赖 SAW。

## GitHub 同步范围

- 本节“云端”指 `https://github.com/eason8319/attention-accelerator-phd`。GitHub 保存可共享的版本，本地保存完整研究资料；服务器到本地的结果回收与本地到 GitHub 的发布是两个独立流程。
- 默认入库：通用源码、学习实验源码、可移植配置与依赖声明、论文/综述的可编辑源码和参考文献库、已有研究文档、人工分析撰写的正式 `REPORT.md`、项目协作规则。遵守下列目录例外；不因为文件是源码就强行加入。
- 保留研究实验目录的既有范围：`research/**/experiments/` 仅入库各实验 `REPORT.md`、直接位于 `experiments/configs/` 的共享配置及根 `.gitkeep`；实验运行脚本与其余产物仍仅本地保留。研究目录中其他通用代码和协议正常入库。
- 默认不入库：原始结果目录（`research/**/results/`、`research/**/outputs/`、`learning/**/results/`、`learning/**/outputs/`）、运行日志、模型权重与缓存、虚拟环境、构建产物、`tmp/`、`output/`、`.tools/`、`.server-sync/`、服务器专用任务提交及环境文件、凭据和本机私有配置。可移植依赖声明不属于服务器私有环境文件。
- 下载的论文 PDF 和 LaTeX 编译产物只在本地保留；模板库既有 PDF 例外继续有效。幻灯片草稿、截图、临时辅助脚本和序列化中间文件不发布；需要版本管理的最终演示文稿及必要可编辑源文件，在明确发布需求后整理到正式目录并检查大小与引用，不整包上传 `tmp/`。
- 报告可引用本地原始结果，但不得将未入库文件描述为 GitHub 上可直接获取。需要公开复现数据、报告必需图或扩展实验代码发布范围时，明确选定文件，再调整忽略规则；不使用强制添加绕过约定。
- 发布前核对实际待提交清单及已跟踪文件；`.gitignore` 不会移除已被 Git 跟踪的文件。不得把设定同步规则等同于已完成提交或推送；按用户发布请求执行，不能自动覆盖本地版本。

## 实验结果与报告

- 学习实验 `learning/p*/` 和研究实验 `research/**/experiments/<name>/` 采用同一组成：现有源码/运行入口、`results/`、`REPORT.md` 和 `experiment.json`。不为统一名字增添无意义的包装脚本；`experiment.json` 统一登记入口、依赖、结果路径/哈希、报告及有效性状态。
- 实验结果相关文件、文件夹及结果归档成员均使用实验用途、模型、设置或版本命名，例如 `metrics_f64/`、`decode_preflight/`、`storage-check.log`；不使用临时里程碑编号命名或描述实验，名称中禁止包含日期时间，前置检查、冒烟测试与运行副本也不例外。新建同步目录同样按用途命名。运行时间保存在 JSON 元数据和日志中；历史原始记录内的时间与原执行路径不伪改。路径变更登记可验证的映射，使用记录哈希核对新位置，保证复现入口可用。重复运行使用有明确含义的新目录，保留非空输出目录保护，验证前不得覆盖有效结果。
- 源码使用 UTF-8、明确命令参数与输出路径；可保留算法需要的子目录。结果统一放 `results/`，构建文件留在 `build/`，保留测试放 `smoke/` 或既有学习实验测试目录。历史原始文件只移动并校验，不重写或伪造字段；异构 JSON/CSV/RTL 文本通过统一清单登记，不强行改成同一数据表。
- 新实验批次保存 `run_config.json`（参数、源码标识、环境与来源）及实际机器结果；结果不能只有空目录或报告摘录。无法取得历史原始数据时应运行适当验证并如实记录当前范围，失败/未验证项不得标为通过。
- 研究实验、重测、冒烟测试及其运行副本统一放在对应 `research/**/experiments/<name>/` 内；不得在通用源码目录或 `.server-sync/` 中建立实验运行目录。
- 通用算法与指标实现可保留在源码模块中；测试代码不得混放在这些模块旁。一次性测试使用临时文件，通过后删除；需要保留的冒烟测试放在对应实验目录内。临时测试产物按同一规则清理，必要结果证据留在实验结果目录。
- 正式报告只写实验目的、当前方法、有效结果、分析与必要局限，不写排错、修复、迁移、部署和新旧实现对照过程。必要维护历史仅简记于既有 CHANGELOG，机器对照证据保留在结果目录，不另建过程报告。
- 每个实验只保留一个正式入口 `REPORT.md`；学习实验位于 `learning/p*/REPORT.md`，研究实验位于 `research/**/experiments/<name>/REPORT.md`。
- 正式报告必须在相应实验完成、原始结果核验后，由作者或助手阅读结果并逐项分析撰写。禁止实验脚本、模板填数程序自动生成、覆盖或追加报告的目的、分析和结论。
- 代码只输出数据、指标、图及机器检查状态；历史自动 Markdown 表格归为数据摘录，不称为实验报告，不替代正式分析。不得把固定结论写进脚本。
- 统一正文顺序：`1. 实验目的`、`2. 方法与设置`、`3. 实验结果`、`4. 分析与讨论`、`5. 局限与有效性`、`6. 结论与后续工作`；开头记录实验日期、整理日期、完成范围与证据来源。
- 目的须说明要回答的问题；方法须写模型/负载、对照、指标、主要参数；结果须能追溯到文件；分析须解释观察与权衡；局限不得省略。
- 未完成实验只登记进度，不写最终报告结论。分批实验可报告已完成批次，必须把未完成批次独立列为待完成，不能混入完整对照或宣称全部完成。
- 历史实验无法补验的数字标为历史记录，不冒充本次重跑。新结果已通过有效性检查、源码/参数/环境足以复现且有实验复现或等价验证依据时，删除被完整取代的归档旧结果；清单记录旧哈希、替代结果和验证依据。仅标记成功或一次小规模冒烟通过不足以替代完整实验；仍需保留的失败证据、独有设置和未覆盖结果不能随归档整包删除。

<a id="literature-registration"></a>

## 相关文献引用与登记（全项目）

- 在本项目讨论、研究计划、实验设计/报告、综述或论文中引用相关文献，或依据作者技术说明/公开实现提出机制、对照或创新判断时，先核对[近年成果对比手册](docs/recent_works_comparison.md)及 [文献台账](docs/lit_watch/ledger.yaml)。以题名、DOI、arXiv ID 和规范 URL 查重；同一工作的版本、别名与作者代码关联到原条目，不重复收录。
- 已有条目核对引用版本、发表状态与本次所用主张；缺失条目在本次任务内按[既有文献流程](docs/lit_watch/README.md)核验并补齐手册总览/卡片和台账，不能只把链接留在对话、计划或报告中。无法核实的来源保留在 inbox 并明确待核实，不编造字段，也不作为已验证结论依据。
- 收录至少登记题名、作者、年份、来源类型/发表状态、DOI/arXiv ID 或作者原始 URL、使用版本、核验日期、具体证据位置及与项目的关系；代码记录作者仓库与所用文档/版本，实际实验另固定提交。论文、预印本、作者技术说明、代码、本项目复现与架构推算分别标识；无 DOI/arXiv 的原始技术说明按类型和规范 URL 登记，不伪造出版信息。
- 量化主张须回到原文，保留基线、模型、平台、上下文/batch、统计口径与限制；摘要和正文、不同版本间的冲突明确记录。正式稿使用 BibTeX 时同步所属既有参考文献库，复用同一来源的既有引用键，不另建平行文献库。
- 交付前核对本次新增/更新的相关引用在手册、台账及所用 BibTeX 中的覆盖与一致性，登记本次核验范围；增量补录不等于全领域检索或全部历史数字复核。规范性要求以本节为准，文献流程、模板和工具规则通过链接引用。

## 文档与产物约束（全项目）

- 默认只更新现有文档，不因一次对话、同步、修复、验证或清理任务新增 Markdown。用户明确要求独立文档，或已完成的新实验需要唯一 `REPORT.md` 时才新增；不得自动复制一套 README/PLAN/NOTES/总结。
- 写入前先查现有入口。同一事实只在一个主文档维护，其他位置用相对链接，不复制状态表或结论段落。
- 根 `README.md` 只负责导航；`docs/research_plan.md` 定义长期方向；R1 已完成，其方法、结果和完成范围由 `research/r1_kv_baseline/REPORT.md` 维护，计量约定由同目录 `protocols/` 维护；`docs/progress/milestones.md` 是当前进度唯一入口；`docs/progress/CHANGELOG.md` 记录历史变更。
- `docs/experiments.md` 只索引实验、结果目录与证据角色；结果、分析和质量问题放在对应实验 `REPORT.md`。通用指标在 `docs/metrics.md`，R1 的具体计量约定在 `research/r1_kv_baseline/protocols/`，避免重复定义。
- 已有论文、参考文献库、阅读笔记、设计推导和文献台账保留各自职责；不为简化目录删除独有研究内容。新增主题优先扩展已有笔记；文献工作流按其既有模板执行。
- 禁止新增独立的清理报告、同步报告、核验报告、交接说明、任务小结、过程日志 Markdown；关键信息写入对应报告或已有 CHANGELOG，机器清单/哈希可放 `.server-sync/<批次>/`，不另配 README。
- 原始结果只保留权威 JSON/CSV 等机器记录及必要日志/图；已有独有证据必须保全。不要再生成可由原始结果直接重建的 `*_data.md`、`*_report.md` 等平行副本。
- 结果去重须同时核对完整文件、汇总中完整覆盖的分片及其引用；尚不能被有效且可复现结果取代的独有历史记录，才按原字节保存在所属实验 `results/archive/`，登记成员、原路径与哈希。满足上一节淘汰条件的旧结果直接删除，不再另建备份副本。有效原始结果不改写；独立产生的 golden/DUT 输出等不同证据角色不能仅因内容相同而合并。续跑入口应能读取完成汇总，并仅在汇总可靠写入、逐字段确认完整覆盖后删除临时检查点。
- 草稿、截图、一次性辅助脚本和构建中间文件放 `tmp/<任务>/`；禁止在项目根目录散落文件。清理时保护最终产物、可编辑源文件和结果；先检查引用及哈希，不按名字猜测可删除。
- 不得删除模型缓存、虚拟环境、Git 历史或已有未提交源码来“整理项目”；可重建的测试缓存与临时依赖可按任务授权清理。
- 删除、移动或合并文档后，修复所有有效相对链接；规范性约束以本文件为准，Cursor 规则只维护格式细节和引用。
