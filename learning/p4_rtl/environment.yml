name: p4-rtl
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - numpy==1.26.4
      - pytest==8.3.5
      # torch 用 CPU 版即可（golden model 生成测试向量）
      # pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu
      - torch==2.5.1
